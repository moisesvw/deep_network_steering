#!/usr/bin/env python

if __name__ == '__main__':
      model_path = '/opt/ml/test.h5' 
    train = Train('/opt/ml/data/driving_log.csv', '/opt/ml/data/', model_path,
                    bath_size=10, epochs=1)

    # A zero exit code causes the job to be marked a Succeeded.
    sys.exit(0)